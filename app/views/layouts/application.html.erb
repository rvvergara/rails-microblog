<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %> | Rails Microblog</title>
    <%= render 'layouts/rails_default'%>
  </head>

  <body>
    <%= render 'layouts/header'%>
    <div class="container">
      <%= yield %>
      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
